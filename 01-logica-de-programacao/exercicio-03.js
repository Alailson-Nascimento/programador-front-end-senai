// Declara a variavel
let valor = 123;

// Criando um loop for

for (let i = 1; i <= valor; i++) {

    // verifica se o resto da divisão por 2 for 0, é par
    if (i % 2 === 0) {
        console.log(i); // mostra no console resultado com o número par
    }
}
